import { WalletChargeDirective } from './wallet-charge.directive';

describe('WalletChargeDirective', () => {
  it('should create an instance', () => {
    // const directive = new WalletChargeDirective();
    // expect(directive).toBeTruthy();
  });
});
